<div class="form-group">
	<label class="col-sm-2 control-label" for="InputText">Disc名</label>
	<div class="col-sm-10">
		<div>
		<%= f.fields_for :discs do |b| %>
			<%= render partial: "admin/products/disc_form", locals: {f: b } %>
		<% end %>
		<%= link_to_add_field("Discを追加", f, :discs, {}) %>
		</div>
	</div>
</div>